
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Cho phép người dùng đã đăng nhập đọc và ghi vào tất cả các đường dẫn
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
